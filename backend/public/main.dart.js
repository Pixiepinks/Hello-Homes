// Placeholder Flutter web bundle committed for Railway /backend-only deploys.
// Regenerate with: scripts/build-flutter-into-backend.sh
(function () {
  var root = document.body || document.documentElement;
  var message = document.createElement('main');
  message.style.cssText = 'font-family: system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif; padding: 2rem; max-width: 48rem; margin: 4rem auto; line-height: 1.5;';
  message.innerHTML = '<h1>Hello Homes</h1><p>The Flutter web bundle is expected here. Rebuild it locally with <code>scripts/build-flutter-into-backend.sh</code> before deployment.</p>';
  root.appendChild(message);
}());
